include:
  - template: Auto-DevOps.gitlab-ci.yml

image: node:current-alpine

pages:
  stage: staging
  script:
    - cd website
    - yarn install
    - yarn build:ci
  variables:
    VITE_GOOGLE_API_KEY: $VITE_GOOGLE_API_KEY
  artifacts:
      paths:
        - public
  only:
    - master
